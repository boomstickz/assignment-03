<%- include("./partials/header.ejs") %>

<h2>Register</h2>

<form action="/auth/register" method="POST">

  <div class="mb-3">
    <label class="form-label">Username</label>
    <input type="text" name="username" class="form-control" required>
  </div>

  <div class="mb-3">
    <label class="form-label">Password</label>
    <input type="password" name="password" class="form-control" required>
  </div>

  <div class="mb-3">
    <label class="form-label">Confirm Password</label>
    <input type="password" name="confirmPassword" class="form-control" required>
  </div>

  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <button class="btn btn-success">Create Account</button>
</form>

<div class="social-login mt-4">
  
  <div class="d-flex flex-wrap gap-3 align-items-center">
    <a class="btn btn-dark social-btn" href="/auth/github">
      <span class="social-icon" aria-hidden="true">🐈</span>
      Register with GitHub
    </a>

    <button
      type="button"
      class="btn btn-outline-light social-btn google-btn"
      onclick="alert('Cloud Google console service is a paid service, student account needs authorization from the university first to implement google login/register.');"
    >
      <span class="social-icon" aria-hidden="true">🔴</span>
      Google Register
    </button>
  </div>

</div>

<%- include("./partials/footer.ejs") %>